package RunTimePolymorphisim;

public class OverView 
{
//basically runtime polymorphisim  means compile bind something else at compile time but 
//at run time result or output get some thing else  so basically its example of 
//method overriding is example compile time binding
}
